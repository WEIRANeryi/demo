2019/9/23
1. mac的使用
    安装webstorm的时候，有一步是需要将webstorm的图标拉出来。
    快捷键需要command键
    开f12需要三个键位配合，shift + command + i/o（明天看下，忘记了是i还是o）
    mac的权限问题 sudo
    mac的全局搜索 command + 空格
    最近国庆不让翻墙，没办法更新组件，必要的时候翻墙更新个组件
2. 百度的初体验
    项目基础重要，使用组件的属性较多，element-ui的各个属性都有用到。
    接口文档中接口众多，且接口使用方式不同，需要时间去适应。
2019/9/24
    对接口的时候可以倒着来，从接口开始，往回翻，找到数据来源。也许数据来源于vuex，多用心去找一下就可以。
配置git的公钥，先输入邮箱和账号，再生成公钥，然后复制公钥到页面上。
    Webstorm的使用，配置服务器，不用每次都npm run dev启动服务，但要注意node npm的版本问题。git之后的第一个按钮用来拉代码，第二个用来commit，之后开命令行push即可，注意百度这里需要相应的语法，push到相应的库。push完看进度，如果没错就等打分，够两分之后合并，然后看wjf的流水线，过了就可以联系后台和pm检查了。
    mac操作命令
    ls查看当前目录 cmd + k清屏
2019/9/25
    提交项目代码出现两次错误，主要原因是自己不够仔细，没有看到具体要求，之后可以对照之前的代码来看具体代码实现的效果，如果需求需要的话要加上，一定要仔细。
    项目检查，自己检查一遍传上去，过检测合并之后发后端检测，后端没问题发PM检测，PM检测之后最终测，最后发布上线.
2019/9/26
    组件模块化开发：可以将表单相关配置信息让后台写接口来返回，通过返回的数据来进行表单的渲染，从而达到模块化开发。
